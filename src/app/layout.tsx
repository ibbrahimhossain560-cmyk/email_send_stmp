import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/Navbar";
import Footer from "@/components/Footer";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NR Mail - SMTP Email Sender Platform | Nafij Rahaman",
  description: "Professional SMTP email sender platform built by Nafij Rahaman. Send beautiful emails with 10+ templates using Brevo SMTP.",
  keywords: "email sender, SMTP, Brevo, email templates, Nafij Rahaman",
  authors: [{ name: "Nafij Rahaman", url: "https://nafij.me" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        <Navbar />
        <main className="min-h-screen">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
